const assets = {
  state: {
    id: "",
    url: "",
    name: ""
  },
  mutations: {
    setId(state, id) {
      state.id = id;
    },
    setUrl(state, url) {
      state.url = url;
    },
    setName(state, name) {
      state.name, name;
    }
  }
};
